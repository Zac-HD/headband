{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "cd $CLAUDE_PROJECT_DIR && uv run pre-commit run --files $(git diff --name-only HEAD) 2>/dev/null || true",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
